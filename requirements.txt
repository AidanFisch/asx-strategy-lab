# Full system (data pipeline + backtesting + validation + live monitor).
# NOTE: vectorbt pulls a numpy-2 stack; pyarrow>=16 and plotly>=5.24 are required
# so their compiled/legacy bits work under numpy 2 (older pins crash on import).
yfinance>=0.2.40
pandas>=2.0
numpy>=2.0
pyarrow>=16
plotly>=5.24
vectorbt>=1.0
ta>=0.11          # ADX / Stochastic indicators (strategies/primitives.py)
scipy>=1.10       # probabilistic Sharpe etc. (robustness.py); vectorbt also needs it
